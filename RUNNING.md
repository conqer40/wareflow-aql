# نظام عقل للأدوات الصحية — التشغيل المحلي

## تشغيل التطوير

```powershell
npm.cmd install
npm.cmd run dev
```

- الواجهة المحلية: `http://127.0.0.1:5173`
- فحص الـ API: `http://127.0.0.1:3001/api/health`

## بيانات الدخول

لا تكتب بيانات الأدمن داخل ملفات المشروع عند النشر. يتم إنشاء أول مدير نظام من متغيرات البيئة:

```env
ADMIN_PHONE=ضع_رقم_الأدمن_في_لوحة_الاستضافة
ADMIN_PASSWORD=ضع_كلمة_مرور_قوية_في_لوحة_الاستضافة
JWT_SECRET=سر_طويل_وعشوائي
```

محليًا فقط، يمكنك استخدام بيانات الاختبار التي اتفقنا عليها داخل بيئة جهازك.

## تشغيل إنتاج محلي

```powershell
npm.cmd run build
$env:NODE_ENV="production"
$env:JWT_SECRET="change-this-secret"
$env:ADMIN_PHONE="01023299755"
$env:ADMIN_PASSWORD="01023299755"
$env:HOST="127.0.0.1"
$env:PORT="7860"
npm.cmd start
```

ثم افتح: `http://127.0.0.1:7860`

## مكونات السيرفر

- تسجيل دخول JWT وصلاحيات مستخدمين.
- SQLite داخل `data/wareflow.db`.
- مستندات واعتمادات ومرفقات PDF/صور/Excel.
- فحص جودة قبل اعتماد إذن الإضافة.
- تحويلات من مخزن إلى مخزن مع المصدر والمستلم.
- توليد PDF وسجل تدقيق.
